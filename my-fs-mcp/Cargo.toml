[package]
name = "my-fs-mcp"
version = "0.1.0"
edition = "2024"

[dependencies]
filesystem-mcp-rust = "0.0.2"
tokio = { version = "1", features = ["full"] } # If needed for your wrapper
anyhow = "1"                                   # For clean error handling
